import { filter as es5filter, map as es5map, reduce as es5reduce } from "es5-collection-methods";
es5map((getType(): numbers[]), el => el + 1);
es5reduce((getType2(): Array<Any>), (acc, el) => acc + el);
es5filter((ev.attachments: any[]), el => el);
